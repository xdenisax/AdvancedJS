# System JS 
  - great for quick use, not for production 
  
  ### Steps for setting up system js
      - insert script tag with jspm
      - insert script for entry point
       ``` 
            System.config({transpiler : 'babel'});
            System.import('./main.js'); //enrty point
       ```
